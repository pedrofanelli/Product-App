spring.application.name=ProyectoInicial

# H2 Console (para que entres desde el navegador a ver las tablas)
# spring.h2.console.enabled=true
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# Mostrar el SQL que genera JPA en la consola de IntelliJ
# spring.jpa.show-sql=true
# spring.jpa.hibernate.ddl-auto=update

# Podemos usar application-test.properties para testing

# docker exec -it postgres_db psql -U user_proyect -d product_db
# Configuración de la conexión a PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/product_db
spring.datasource.username=user_proyect
spring.datasource.password=pass123
spring.datasource.driverClassName=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#logging.level.org.springframework.jdbc=DEBUG
#logging.level.org.hibernate.engine.jdbc.env.internal=DEBUG
#logging.level.web=DEBUG
#logging.level.com.zaxxer.hikari=DEBUG

# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.database=POSTGRESQL
# spring.datasource.platform=postgres
# spring.jpa.show-sql=true
# spring.datasource.testWhileIdle=true
# spring.datasource.validationQuery=SELECT 1